export type StoreStateDataType = {
    sysdate: string,
    systime: string,
    title: string,
    currTab: string,
    isLogin: boolean,
    isMobile: boolean
}

export type StoreStateViewType = {

}

export type StoreStateType = {
    
}