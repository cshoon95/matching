
// reducer 
export const initialViewState: StoreStateViewType = {

}

const View = (state = initialViewState, action: any) {
    switch (action.type) {

        default:
            return state;
    }
}